let a = { prop: 1 };

function suma(n) {
    n.prop++;
}
suma(a)
console.log(a);


/**
 * primitivos se copian cuando se asignana a otra variables
 * referecia se crea una referencian
 *  -objetos
 *  -array
 *  -funciones
 */ 